spring:
  servlet:
    multipart:
      max-file-size: 100MB      # 单个文件最大大小
      max-request-size: 100MB   # 单次请求总大小(含多个文件)
  # 数据源配置 - MySQL
  datasource:
    url: jdbc:mysql://182.43.87.123:3306/nova_demo?useSSL=false&serverTimezone=Asia/Shanghai&characterEncoding=utf8&allowPublicKeyRetrieval=true
    username: root
    password: '#Xynet2022'
    driver-class-name: com.mysql.cj.jdbc.Driver
    hikari:
      max-lifetime: 600000  # 单位毫秒
      keepalive-time: 300000  # 单位毫秒，5分钟
      connection-timeout: 5000  # 5秒
      idle-timeout: 300000  # 5分钟
      minimum-idle: 5
      maximum-pool-size: 20
  # Redis 配置
  data:
    redis:
      host: 182.43.87.123
      port: 9008
      password: xy20220101
      database: 0
      timeout: 5000

# MyBatis-Plus 配置
mybatis-plus:
  configuration:
    map-underscore-to-camel-case: true
    #log-impl: org.apache.ibatis.logging.stdout.StdOutImpl

  # 静态资源配置（使用 Spring Boot 默认位置：public/、static/、resources/、META-INF/resources/）
  web:
    resources:
      add-mappings: true

# 服务器配置
server:
  port: 8080
  servlet:
    context-path: /
  error:
    include-message: always