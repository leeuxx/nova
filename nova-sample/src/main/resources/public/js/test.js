// param     — 静态参数（字符串）
// transmitParams — 表单字段键值对（对象）
// $btn      — 按钮的 jQuery 对象（如果按钮有 id）

console.log('param:', param, 'transmitParams:', transmitParams, '$btn:', $btn)

const id = window.popup.modal('https://ai.mxrvending.com')
